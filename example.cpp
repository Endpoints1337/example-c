#Include <iostream> // basic input output for c++
#Include <windows.h> // use this if you are on windows just helps with beginer stuff

// y0u most likely will use more header files then  i used.
  static bool func = false;

void example(func) 
{    
  if (funct)
  {
    cout << "Hello World!"; // this will just print hello world!
    printf("Hello World!"); // same thing but for only windows.
  }
}

// This is the main function so this is where your code will beging you can make varaibles to change this tho
int main() 
{  
  example(); // by making a void above the function you will begin to have a varaible.
}
